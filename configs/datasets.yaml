# -----------------------------------------------------------------------
# Dataset configuration for the 5 chest X-ray datasets.
#
# For each dataset you need:
#   val_test.csv        : path(s) to CSV file(s) with columns [Path, label]
#                         pass a list to merge val.csv + test.csv automatically
#   val_test.image_root : (optional) prefix prepended to relative paths in CSV
#   golden.images_dir   : folder containing the golden-set images
#   golden.annotations_dir : folder containing .txt annotation files
# -----------------------------------------------------------------------

datasets:

  dataset1:
    val_test:
      csv:
        - /data/dataset1/val.csv
        - /data/dataset1/test.csv
      image_root: /data/dataset1          # set to null if CSV has absolute paths
    golden:
      images_dir:      /data/dataset1/golden/images
      annotations_dir: /data/dataset1/golden/annotations

  dataset2:
    val_test:
      csv:
        - /data/dataset2/val.csv
        - /data/dataset2/test.csv
      image_root: /data/dataset2
    golden:
      images_dir:      /data/dataset2/golden/images
      annotations_dir: /data/dataset2/golden/annotations

  dataset3:
    val_test:
      csv:
        - /data/dataset3/val.csv
        - /data/dataset3/test.csv
      image_root: /data/dataset3
    golden:
      images_dir:      /data/dataset3/golden/images
      annotations_dir: /data/dataset3/golden/annotations

  dataset4:
    val_test:
      csv:
        - /data/dataset4/val.csv
        - /data/dataset4/test.csv
      image_root: /data/dataset4
    golden:
      images_dir:      /data/dataset4/golden/images
      annotations_dir: /data/dataset4/golden/annotations

  dataset5:
    val_test:
      csv:
        - /data/dataset5/val.csv
        - /data/dataset5/test.csv
      image_root: /data/dataset5
    golden:
      images_dir:      /data/dataset5/golden/images
      annotations_dir: /data/dataset5/golden/annotations
